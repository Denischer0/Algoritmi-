#include <iostream>
#include <vector>
using namespace std;

int main() {
	vector<int> A = { 0,12,32,22,832,-12,18,11,-243,-48,4,1 };
	int right = A.size();
	int left = 0;
	int mi = 10 ^ 10;
	int d = 0;
	for (int i = 0; i < right; i++) {
		for (int j = left; j < right; j++) {
			if (A[i]>A[j] and d==0) {
				swap(A[j], A[j + 1]);
				d=1;
			}
			if (d == 1)
			{
				if (A[j] > A[j + 1]) {
					swap(A[j], A[j + 1]);
				}
			}
			
		}
	}
}
