#include <iostream>
#include <vector>
#include <string>
using namespace std;
void print(vector<int> x, vector<int> y) {
	cout << endl;
	for (int i = 0; i < x.size(); i++) {
		cout << x[i] << " ";
	}
	cout << endl;
	for (int i = 0; i < y.size(); i++) {
		cout << y[i] << " ";
	}
}
vector<int> reverse(vector<int> x) {
	vector<int> y = {};
	for (int i = x.size() - 1; i >= 0; i--) {
		y.push_back(x[i]);
	}
	return y;
}
int main() {
	cout << "Vvedite pervoe dvoichnoe chislo" << endl;
	string a, b;
	vector<int> ch1{ 0 }, ch2 = { 0 };
	cin >> a;
	cout << "Vvedite vtoroe dvoichnoe chislo" << endl;
	cin >> b;
	string c = "";
	if (a.size() > b.size()) {
		for (int i = 0; i < (a.size() - b.size()); i++) {
			c += '0';
		}
		b = c + b;
		for (int i = 0; i < a.size(); i++) {
			ch1.push_back((a[i] - '0'));
		}
		for (int i = 0; i < b.size(); i++) {
			ch2.push_back(b[i] - '0');
		}
	}
	else if (a.size() < b.size()) {
		for (int i = 0; i < (b.size() - a.size()); i++) {
			c += '0';
		}
		a = c + a;
		for (int i = 0; i < a.size(); i++) {
			ch2.push_back(a[i] - '0');
		}
		for (int i = 0; i < b.size(); i++) {
			ch1.push_back(b[i] - '0');
		}
	}
	else if (a.size() == b.size()) {
		for (int i = 0; i < a.size(); i++) {
			ch1.push_back((a[i] - '0'));
		}
		for (int i = 0; i < b.size(); i++) {
			ch2.push_back(b[i] - '0');
		}
	}
	print(ch1, ch2);
	cout << endl;
	vector<int> ch3 = {}, ch4 = {};
	int k = 0, l = ch1.size();
	while (ch2 != vector<int>(l, 0)) {
		for (int i = ch1.size() - 1; i >= 0; i--) {
			if (k == 0) {
				if (ch1[i] + ch2[i] == 10) {
					ch3.push_back(0);
					ch4.push_back(0);
					k++;
				}
				else if (ch1[i] + ch2[i] < 10) {
					ch3.push_back(ch1[i] + ch2[i]);
					ch4.push_back(0);
				}
				else if (ch1[i] + ch2[i] > 10) {
					ch3.push_back(ch1[i] + ch2[i] - 10);
					ch4.push_back(0);
					k++;
				}

			}
			else if (k == 1) {
				if (ch1[i] + ch2[i] == 10) {
					ch3.push_back(0);
					ch4.push_back(1);
					k++;
				}
				else if ((ch1[i] + ch2[i] < 10) and ch2[i]<9) {
					ch3.push_back(ch1[i] + ch2[i]+1);
					ch4.push_back(0);
				}
				else if ((ch1[i] + ch2[i] < 10) and ch2[i] == 9) {
					ch3.push_back(0);
					ch4.push_back(0);
					k++;
				}
				else if (ch1[i] + ch2[i] > 10) {
					ch3.push_back(ch1[i] + ch2[i] - 9);
					ch4.push_back(0);
					k++;
				}
				k--;
			}
		}
		ch1 = reverse(ch3);
		ch2 = reverse(ch4);
		print(ch1, ch2);
		cout << endl;
		ch3 = {};
		ch4 = {};
	}

}

