#include <iostream>
#include <vector>
#include <string>
using namespace std;
void print(vector<int> x,vector<int> y) {
	cout << endl;
	for (int i = 0; i < x.size(); i++) {
		cout << x[i] << " ";
	}
	cout << endl;
	for (int i = 0; i < y.size(); i++) {
		cout << y[i] << " ";
	}
}
int main() {
	cout << "Vvedite pervoe dvoichnoe chislo" << endl;
	string a,b;
	vector<int> ch1{}, ch2 = {};
	cin >> a;
	cout << "Vvedite vtoroe dvoichnoe chislo" << endl;
	cin >> b;
	for (int i = 0; i < a.size(); i++) {
		if (a[i] == '0' or a[i] == '1') {
		}
		else{
			cout << "Chisla vvedeni nekorektno";
			exit(0);
		}
	}
	for (int i = 0; i < b.size(); i++) {
		if (b[i] == '0' or b[i] == '1') {
		}
		else {
			cout << "Chisla vvedeni nekorektno";
			exit(0);
		}
	}
	string c="";
	if (a.size() > b.size()) {
		for (int i = 0; i < (a.size() - b.size()); i++) {
			c += '0';
		}
		b = c + b;
		for (int i = 0; i < a.size(); i++) {
			ch1.push_back((a[i] - '0'));
		}
		for (int i = 0; i < b.size(); i++) {
			ch2.push_back(b[i] - '0');
		}
	}
	if (a.size() < b.size()) {
		for (int i = 0; i < (b.size() - a.size()); i++) {
			c += '0';
		}
		a = c + a;
		for (int i = 0; i < a.size(); i++) {
			ch2.push_back(a[i] - '0');
		}
		for (int i = 0; i < b.size(); i++) {
			ch1.push_back(b[i] - '0');
		}
	}
	print(ch1, ch2);
	for (int i = ch1.size() - 1; i >= 0; i--) {
		if (ch1[i] + ch2[i]) {
			ch2[i + 1]++;
			ch2[i]--;
			ch1[i]--;
		}
		else {
			ch1[i] += ch2[i];
			ch2[i]='0';
		}
		print(ch1, ch2);
	}
}


